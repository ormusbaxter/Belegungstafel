# Nur für die Prüfungen; die Tafel selbst braucht keine Abhängigkeiten.
tests/node_modules/
tests/package-lock.json

# Ausgaben von Testläufen
tests/screenshots/

# Erzeugte Auslieferungsarchive (siehe release.sh)
dist/
